version: 2

sources:
  - name: arcgis_raw
    database: rj-smas
    schema: arcgis_raw
    tables:
      - name: cras_cas_poligonos_smas_raw

models:
  - name: poligonos_rmi
    description: "Tabela de polígonos CRAS e CAS"
    columns:
      - name: fid
        description: "fid definido pelo Arcgis"
        tests:
          - not_null
          - unique
      - name: SHAPE_Area
        description: "Área do polígono"