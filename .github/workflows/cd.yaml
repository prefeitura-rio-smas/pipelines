name: Register flows (production)

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/cd.yaml"
      - ".github/workflows/build_docker.yaml"
      - ".github/workflows/scripts/**"
      - "pipelines/**/*"
      - "pyproject.toml"
      - "Dockerfile"

jobs:
  register-flows:
    name: Register flows (production)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Wait for Docker image to be available
        uses: lewagon/wait-on-check-action@v1.3.1
        with:
          ref: ${{ github.ref }}
          check-name: 'Build Docker image'
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          verbose: true

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Set up Python
        run: uv python install 3.13

      - name: Install dependencies
        run: uv sync

      - name: Authenticate Prefect
        env:
          PREFECT_API_URL: ${{ secrets.PREFECT_API_URL }}
          PREFECT_API_AUTH_STRING: ${{ secrets.PREFECT_API_AUTH_STRING }}
        run: |
          uv run prefect config set PREFECT_API_URL="$PREFECT_API_URL"
          uv run prefect config set PREFECT_API_AUTH_STRING="$PREFECT_API_AUTH_STRING"

      - name: Register Prefect flows
        env:
          IMAGE_TAG: ${{ github.sha }}
          PREFECT_API_URL: ${{ secrets.PREFECT_API_URL }}
          PREFECT_API_AUTH_STRING: ${{ secrets.PREFECT_API_AUTH_STRING }}
          GCP_CREDENTIALS: ${{ secrets.GCP_CREDENTIALS }}
          SIURB_URL: ${{ secrets.SIURB_URL }}
          SIURB_USER: ${{ secrets.SIURB_USER }}
          SIURB_PWD: ${{ secrets.SIURB_PWD }}
        run: |
          uv run python .github/workflows/scripts/register_flows.py --env prod --image-tag $IMAGE_TAG